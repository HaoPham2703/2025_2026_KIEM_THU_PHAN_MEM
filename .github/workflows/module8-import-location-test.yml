name: Module8 - Import & Location Test

on:
  push:
    branches: [weblau]
  pull_request:
    branches: [weblau]

jobs:
  test-module8-import-location:
    name: 📦 Module8 - Import & Location Test Suite
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18.x"
          cache: "npm"
          cache-dependency-path: Back-end/package-lock.json

      - name: 📦 Install dependencies
        working-directory: ./Back-end
        run: npm install

      - name: 🧪 Run Module8 Import & Location Tests
        working-directory: ./Back-end
        run: |
          echo "📦 Testing Module8 - Import & Location Management..."
          npm test -- importController.test.js locationController.test.js --json --outputFile=module8-test-results.json || true
        continue-on-error: true
        timeout-minutes: 5
        env:
          NODE_ENV: test
          JWT_SECRET: test-jwt-secret-key-for-module8-testing
          JWT_EXPIRES_IN: 90d
          JWT_COOKIE_EXPIRES_IN: 90

      - name: 📊 Generate Module8 Test Report
        if: always()
        working-directory: ./Back-end
        run: |
          echo "# 📦 Module8 - Import & Location Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 📋 Test Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ -f module8-test-results.json ]; then
            NUM_TESTS=$(node -p "require('./module8-test-results.json').numTotalTests || 0")
            NUM_PASSED=$(node -p "require('./module8-test-results.json').numPassedTests || 0")
            NUM_FAILED=$(node -p "require('./module8-test-results.json').numFailedTests || 0")
            
            echo "| Metric | Value |" >> $GITHUB_STEP_SUMMARY
            echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
            echo "| **Total Tests** | $NUM_TESTS |" >> $GITHUB_STEP_SUMMARY
            echo "| **✅ Passed** | $NUM_PASSED |" >> $GITHUB_STEP_SUMMARY
            echo "| **❌ Failed** | $NUM_FAILED |" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            
            if [ "$NUM_FAILED" != "0" ]; then
              echo "## ❌ Failed Tests" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
              echo '```json' >> $GITHUB_STEP_SUMMARY
              node -e "
                const results = require('./module8-test-results.json');
                if (results.testResults) {
                  results.testResults.forEach(file => {
                    if (file.assertionResults) {
                      file.assertionResults
                        .filter(test => test.status === 'failed')
                        .forEach(test => {
                          console.log('Test: ' + test.fullName);
                          if (test.failureMessages && test.failureMessages.length > 0) {
                            console.log('Error: ' + test.failureMessages[0].split('\\n')[0]);
                          }
                          console.log('---');
                        });
                    }
                  });
                }
              " >> $GITHUB_STEP_SUMMARY || echo "No detailed error info available" >> $GITHUB_STEP_SUMMARY
              echo '```' >> $GITHUB_STEP_SUMMARY
            else
              echo "## ✅ All Tests Passed!" >> $GITHUB_STEP_SUMMARY
            fi
          else
            echo "⚠️ Test results file not found. Check logs above." >> $GITHUB_STEP_SUMMARY
          fi

      - name: 📤 Upload Module8 Test Results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: module8-import-location-test-results
          path: Back-end/module8-test-results.json
          retention-days: 30

      - name: 📊 Generate Coverage for Module8
        working-directory: ./Back-end
        run: |
          echo "Generating coverage report for Module8..."
          npm test -- importController.test.js locationController.test.js --coverage --coverageDirectory=./coverage-module8
        continue-on-error: true

      - name: 📤 Upload Module8 Coverage
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: module8-import-location-coverage
          path: Back-end/coverage-module8
          retention-days: 30

      - name: 💬 Comment PR with Results
        if: github.event_name == 'pull_request' && always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            let comment = '## 📦 Module8 - Import & Location Test Results\n\n';
            
            try {
              const results = JSON.parse(fs.readFileSync('./Back-end/module8-test-results.json', 'utf8'));
              const numTests = results.numTotalTests || 0;
              const numPassed = results.numPassedTests || 0;
              const numFailed = results.numFailedTests || 0;
              
              comment += '| Metric | Value |\n';
              comment += '|--------|-------|\n';
              comment += `| **Total Tests** | ${numTests} |\n`;
              comment += `| **✅ Passed** | ${numPassed} |\n`;
              comment += `| **❌ Failed** | ${numFailed} |\n\n`;
              
              if (numFailed > 0) {
                comment += '### ❌ Failed Tests\n\n';
                results.testResults.forEach(file => {
                  file.assertionResults
                    .filter(test => test.status === 'failed')
                    .forEach(test => {
                      comment += `- **${test.fullName}**\n`;
                      if (test.failureMessages && test.failureMessages.length > 0) {
                        comment += `  \`\`\`\n  ${test.failureMessages[0].split('\n')[0]}\n  \`\`\`\n`;
                      }
                    });
                });
              } else {
                comment += '### ✅ All Tests Passed!\n';
              }
            } catch (error) {
              comment += '⚠️ Could not read test results.\n';
            }
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });

      - name: ❌ Fail if tests failed
        if: always()
        working-directory: ./Back-end
        run: |
          if [ -f module8-test-results.json ]; then
            NUM_FAILED=$(node -p "require('./module8-test-results.json').numFailedTests || 0")
            if [ "$NUM_FAILED" != "0" ]; then
              echo "❌ $NUM_FAILED test(s) failed!"
              exit 1
            else
              echo "✅ All tests passed!"
            fi
          else
            echo "⚠️ Test results not found, assuming failure"
            exit 1
          fi


